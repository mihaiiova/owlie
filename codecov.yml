coverage:
  status:
    project:
      default:
        # Compare against the base commit: fail only when coverage drops,
        # with no absolute floor while the codebase matures.
        target: auto
        threshold: 0%
    patch:
      default:
        target: auto
