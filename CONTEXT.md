# Owlie content extraction

Owlie CLI locally extracts source content into provider-neutral normalized documents, then optionally processes those documents with an LLM.

## Language

**Article**:
A direct HTTP(S) page whose server-rendered HTML contains readable editorial content. It is extracted without browser rendering or generic webpage crawling.
_Avoid_: Web, webpage, generic page
